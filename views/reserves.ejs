<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Benvingut</title>
    <link rel="stylesheet" type= "text/css" href="style.css">
</head>

<body>
    <header>
        <h1>Gestió de reserves per <%= user.name %>!</h1>
        <h2><%- htmlMessage %></h2>
    </header>
    <main>
        <section>
            <h2>Llista de Reserves</h2>
            <ul class="reserva-list">
                <% data.reserves.forEach(reserva => { %>
                    <li class="product-item">
                        <strong>Estado: <%= reserva.estat %></strong>
                        <span>Reserva data: <%= reserva.data %></span>
                        <span> <a href="/reserves/<%=reserva.id%>">Detall de recurso</a></span>
                        <span> <a href="/reserves/<%= reserva.id %>/edit" class="btn-edit">Editar Reserva</a></span>
                    </li>
                <% }) %>
            </ul>
        </section>
    </main>
</body>
</html>