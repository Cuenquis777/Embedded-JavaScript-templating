<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Benvingut</title>
    <link rel="stylesheet" type= "text/css" href="style.css">
</head>

<body>
    <header>
        <h1>Gestió de book per <%= user.name %>!</h1>
        <h2><%- htmlMessage %></h2>
    </header>
    <main>
        <section>
            <h2>Llista de Llibres</h2>
            <ul class="book-list">
                <% data.books.forEach(book => { %>
                    <li class="product-item">
                        <strong><%= book.nombre %></strong>
                        <span>Categoria: <%= book.categoria %></span>
                        <span>Preu: €<%= book.precio %></span>
                        <span> <a href="/books/<%=book.id%>">Detall de reserva</a></span>
                    </li>
                <% }) %>
            </ul>
        </section>
    </main>
</body>
</html>